import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Xibalba Shield — Cryptographic HIPAA Compliance-as-a-Service",
  description: "Hard mathematical guardrails for medical AI agents. Prevent patient PHI leaks, secure autonomous clinical billing, and log zero-knowledge audit proofs using the Integrity Protocol ($ITK). Deployable across enterprise hospitals, private clinics, pharmacies, and telemedicine networks.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
