# Xibalba Shield — Audit Status

Status: AUDIT UPDATED · 2026-08-06

**Update — 2026-08-12:** This snapshot is pinned to commit `d8acc3c7`; its numbers are left
as-recorded rather than rewritten. Two things below are now stale and corrected here rather than
in place: the root-free suite is currently **118 passed, 9 skipped** (grew by the ActionBroker-
wiring tests added 2026-08-12), and `pyproject.toml`'s `integrity-sdk` dependency is a local
filesystem path (`@ file:///home/xibalba/Projects/integrity-core/integrity-sdk`), not a pinned
git commit as line 35 below states — the comment above that dependency line described a git
pin that was never actually what got declared. Current status lives in `README.md` and
`docs/archive/2026-08/IMPLEMENTATION_PLAN.md`.

**Update — 2026-08-21:** Current root-free validation is **138 passed, 9 skipped** after Rego
profile coverage and CLI Tier-2 SLM backend wiring tests landed. The historical audit numbers
below remain pinned to their original run.

The consolidated cross-repository implementation plan is `/home/xibalba/Documents/INTEGRITY — Cross-Repository Audit and Implementation Plan.md`.

## Verified state

- Clean default-branch audit commit: `d8acc3c7cf6888f1d682d221f413a0d4edb15bf5`.
- Root-free suite: 103 passed, 7 skipped.
- New local closure work: policy bundle version/hash is attached to decisions, export status is visible in local decisions/CLI output, file-write sensitive-path filtering is config-loadable, default policy packs validate, trusted policy hash enforcement exists, systemd packaging/runbook exists, root-free CI exists, and the `integrity-sdk` dependency is pinned to commit `bed263a8001ad6c975260d29f1876d8f800f7d2a`.
- Policy engine, schemas, agent core, guardrail hooks, exporter wire format, CLI, local configuration, and hot reload are implemented and tested.
- The repository documents historical live verification of Linux process-exec and file-write probes; the delegated audit did not reproduce those claims because it was non-root and had no live BCC middleware.
- The delegated audit freshly verified a synthetic no-exporter CLI run and a local Docker build/dev-mode execution only.

## Specification reconciliation

The parent protocol specification historically stated that the Shield repository and modules were entirely planned. That status is stale relative to the current implementation. The normative design remains authoritative for behavior; this audit status and the living README are authoritative for observed implementation.

## Tasks

- [ ] Reconcile the normative Shield specification status with the implementation map.
- [ ] Resolve TCP-connect verification using a supported BCC/kernel path or BTF-verified implementation; do not guess offsets.
- [ ] Register the exporter DID with the oracle and verify the complete path.
- [ ] Re-run resource measurements with a registered DID and real eBPF overhead.
- [x] Add free/open-source CI for root-free tests and explicit root-gated results.
- [ ] Keep Windows/macOS, DNS, content classification, tenant cloud API, signed code update, compliance reporting, and pilots marked planned or blocked.
- [x] Resolve the specification wording inconsistency between five and six guardrail hooks in repo-local SPECIFICATION/README; six hook points are authoritative.

## Documentation drift found

- `docs/archive/2026-08-06/HANDOFF.md` is historical and contains stale test counts (`58 passed, 6 skipped`) versus the current root-free run (`103 passed, 7 skipped`).
- `shield/sensors/base.py` and `CLAUDE.md` have been reconciled with the README's historical eBPF verification status.
- No tracked `xibalba_shield.egg-info/PKG-INFO` metadata remains in this tree.
- `pyproject.toml` now pins `integrity-sdk` to a reviewed git commit.

## Production posture

Xibalba Shield is a real Linux-first endpoint-agent prototype, not production-ready. Historical eBPF/exporter observations are not equivalent to current live verification.
